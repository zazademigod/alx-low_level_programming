/*this is a program to print a message to the console using puts with the 
 * with the first single quote appearing after using the (\) so that the fiel ignores it*/
#include <stdio.h>

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
